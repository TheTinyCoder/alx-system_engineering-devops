#!/usr/bin/env bash
# configures an Ubuntu server to change hosts in hosts file
cp /etc/hosts ~/hosts.new
sed -i "/\tlocalhost/b 127.0.0.2" ~/hosts.new
sed -i "/\tfacebook.com/b 8.8.8.8" ~/hosts.new
cp -f ~/hosts.new /etc/hosts
